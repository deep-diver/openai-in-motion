# Second set of ten refinement rounds · 2026-09-08

Baseline: the bilingual edition at f866c6f. These ten bounded iterations preserve the 44-scene history, fixed orthographic camera, single stage, continuous scene clock, and maximum of two visible annotation panels. No historical dates, sources or model claims were added.

1. **Pause offscreen playback.** Page visibility suspends the scene clock while another tab is in front. Returning resumes only when the viewer had been playing; explicit pause remains paused. A visibility regression covers both cases.
2. **Give captions their own space.** Replaced overlapping absolute stage/caption placement with two grid rows. All three caption lines contribute their natural height, and their crossfade travel uses that height rather than a fixed 88 pixels. Korean, English and enlarged text can extend the page without reducing the stage to fit. Reviewed the responsive cascade and retained both languages' rendered narration checks.
3. **Respect focused controls.** Switches, comboboxes, spinbuttons, editable regions and scrollable annotations retain native input. Shift+arrow no longer changes scenes. Added keyboard regressions alongside existing touch, wheel and focused-slider cases.
4. **Move the archive playhead continuously.** The line now advances with progress inside each scene and meets the next scene at the same position. It stops inside the final cell and disables smoothing for reduced motion. A rendered-component regression checks these boundaries.
5. **Separate long annotations.** The two visible panels are measured and placed apart when their boxes collide. If enlarged text exceeds available height, its panel remains scrollable and keyboard accessible instead of shrinking the text. Leaders follow the resolved boxes. Geometry checks cover phone/tablet/desktop widths, short stages and enlarged copy; the original two-panel timing test remains in force.
6. **Make CLIP's choice visible.** A marker compares the two candidate captions and stops on the dog description; a highlight then confirms the match before the held result. Sequence and backward-seek checks pass.
7. **Show Dactyl gripping throughout.** Moved the hand's entrance to the first beat so the initial grip is visible. Alternating fingertips reposition while opposing fingers hold the block; the thumb participates and the hand retains the final orientation. Checked first-beat visibility, alternating grip and final pose.
8. **Give GPT-4o a readable process.** The microphone responds, a scan moves over the equation, the intermediate step `3x = 9` appears, and then `x = 3` is revealed. The mic ring returns to its resting size. Checked ordering and completed state.
9. **Complete the computer-use gesture.** The email request is underlined, the cursor moves to the calendar slot, a brief click ring appears, and only then is the meeting block shown. Cursor tip and click marker align; transient feedback disappears while the result remains. Checked ordering and rewind.
10. **Let Gym settle.** CartPole's cart and pole now make coordinated, damped corrections instead of continuing unrelated oscillations. Both settle at the center for the completed scene. Checked continuity, decreasing amplitude, final rest and deterministic seeking.

Updated Korean and English staging captions for the five affected scenes. Reviewed new prop depth/placement and retained the original no-shrink-at-beat-three regressions. Validation uses component, geometry and deterministic GSAP tests, TypeScript, app lint and the production build. Browser visual QA was not requested and was not run.
